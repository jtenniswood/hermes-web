# Hermes Web (hermes-mobile) — root ignore
node_modules/
dist/
result/
.env
*.log
PLAN.md

# Renderer sources mounted by `nix develop` from the pinned upstream input
# (never committed — they come from the flake input at dev/build time).
apps/desktop
apps/shared

# Web build artifacts
apps/web-desktop/dist/
apps/web-desktop/logs/

/result

# Exact renderer checkout prepared from flake.lock
.renderer/

# Browser test output
test-results/
playwright-report/
