name: browser-preview
on:
  pull_request:
permissions:
  contents: read
concurrency:
  group: browser-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  review-image:
    if: github.event.pull_request.head.repo.full_name == github.repository && contains(github.event.pull_request.labels.*.name, 'preview-image')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: corepack enable
      - run: node scripts/renderer.mjs
      - run: pnpm install --frozen-lockfile
      - id: identity
        name: Identify the review build
        run: |
          {
            echo "image=ghcr.io/${GITHUB_REPOSITORY,,}"
            echo "tag=pr-${{ github.event.pull_request.number }}-$(git rev-parse HEAD)-$(node scripts/renderer.mjs --revision)"
            echo "renderer=$(node scripts/renderer.mjs --revision)"
            echo "lock_hash=$(sha256sum pnpm-lock.yaml | cut -d' ' -f1)"
          } >> "$GITHUB_OUTPUT"
      - uses: docker/setup-buildx-action@v3
      - name: Build browser preview nginx image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ${{ steps.identity.outputs.image }}:${{ steps.identity.outputs.tag }}
          build-args: |
            HERMES_WRAPPER_REV=${{ github.event.pull_request.head.sha }}
            HERMES_RELEASE_CHANNEL=browser-preview
          labels: |
            org.opencontainers.image.revision=${{ github.event.pull_request.head.sha }}
            io.hermes.renderer.revision=${{ steps.identity.outputs.renderer }}
            io.hermes.dependency-lock.sha256=${{ steps.identity.outputs.lock_hash }}
            io.hermes.release.channel=browser-preview
          cache-from: type=gha,scope=browser-preview
          cache-to: type=gha,mode=max,scope=browser-preview
      - run: pnpm exec playwright install --with-deps chromium
      - name: Test the browser experience through the built nginx image
        run: pnpm exec playwright test tests/browser/browser.spec.mjs
        env:
          HERMES_TEST_IMAGE: ${{ steps.identity.outputs.image }}:${{ steps.identity.outputs.tag }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-preview-evidence-${{ github.event.pull_request.head.sha }}
          path: |
            test-results
            playwright-report
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish the tested review image
        env:
          IMAGE: ${{ steps.identity.outputs.image }}:${{ steps.identity.outputs.tag }}
        run: docker push "$IMAGE"
      - name: Publish the matching synthetic gateway
        uses: docker/build-push-action@v6
        with:
          context: .
          target: preview-gateway
          push: true
          tags: ${{ steps.identity.outputs.image }}:${{ steps.identity.outputs.tag }}-gateway
          cache-from: type=gha,scope=browser-preview
      - name: Record runnable images
        env:
          IMAGE: ${{ steps.identity.outputs.image }}:${{ steps.identity.outputs.tag }}
        run: |
          echo "HERMES_BROWSER_PREVIEW_IMAGE=$IMAGE" > browser-preview-images.env
          echo "HERMES_PREVIEW_GATEWAY_IMAGE=$IMAGE-gateway" >> browser-preview-images.env
          cat browser-preview-images.env >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: browser-preview-images-${{ github.event.pull_request.head.sha }}
          path: browser-preview-images.env
